# Core dependencies
requests>=2.31.0
pandas>=2.0.0
biopython>=1.81
numpy>=1.24.0
tqdm>=4.65.0
python-dateutil>=2.8.2

# Ontology and RDF handling
owlready2>=0.43
rdflib>=6.3.0

# OBO ontology parsing (for Disease Ontology, Gene Ontology, Uberon)
obonet>=0.3.0
pronto>=2.5.0

# Caching for API requests
requests-cache>=1.0.0

# Database connections
mysql-connector-python>=8.0.33

# Environment variables
python-dotenv>=1.0.0

# ista dependencies (installed separately from source)
# mysqlclient>=2.2.0  # Installed with ista
# openpyxl>=3.1.0  # Installed with ista

# Web scraping (for DrugBank)
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Optional: for better progress bars
rich>=13.0.0
